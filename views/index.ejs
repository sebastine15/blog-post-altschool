<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Welcome to Sebastine's Blogging Website! Explore the latest posts and articles on various topics.">
  <meta name="keywords" content="blog, articles, Alt_School Africa, Sebastine, latest posts">
  <meta name="author" content="Sebastine">
  <link rel="stylesheet" href="/css/styles.css">
  <title>My Blogging Website</title>
</head>
<body>
  <header>
    <div class="author">
      <h1 class="author__heading">My Blogging Website</h1>
      <p class="author__body">I am Sebastine, a Student at Alt_School Africa</p>
    </div>
    <img 
      src="/img/businessblog-1.jpg" 
      alt="Newspaper featuring a scrabble tag" 
      class="hero-image" 
      width="981" 
      height="528">
  </header>

  <main>
    <section class="articles">
      <h2 class="articles__heading">Latest Posts</h2>
      
      <ul class="article-ul">
        <% if (articles.length > 0) { %>
          <% articles.forEach(article => { %>
            <li class="article-list">
              <a href="/article/<%= article._id %>" class="article-link">
                <h3 class="article-title"><%= article.title %></h3>
                <div class="article-meta">
                  <span class="article-date"><%= article.createdAt.toDateString() %></span>
                  <span class="article-reading-time"><%= article.readingTime %> mins</span>
                  <span class="article-read-count">
                    <i class="fas fa-eye"></i> <%= article.readCount %>
                  </span>
                </div>
              </a>
            </li>
          <% }) %>
        <% } else { %>
          <li class="no-articles">No articles available at the moment. Please check back later!</li>
        <% } %>
      </ul>

      <div class="pagination__container">
        <% if (prevPage !== null) { %>
          <a href="/?page=<%= prevPage %>" class="pagination">View Newer Articles &gt;</a>
        <% } %>
        <% if (nextPage !== null) { %>
          <a href="/?page=<%= nextPage %>" class="pagination">&lt; View Older Articles</a>
        <% } %>
      </div>
    </section>
  </main>

  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Sebastine's Blogging Website. All rights reserved.</p>
  </footer>

  <!-- Include any external JavaScript or FontAwesome -->
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html>
